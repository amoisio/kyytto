FROM mysql:5.7

ARG ROOT_PASSWORD
ARG DATABASE=kyytto
ARG USER=kyytto
ARG PASSWORD=Kyytto123

ENV MYSQL_ROOT_PASSWORD=${ROOT_PASSWORD}  \
    MYSQL_DATABASE=${DATABASE} \
    MYSQL_USER=${USER} \
    MYSQL_PASSWORD=${PASSWORD} 

WORKDIR /app
ADD /database/* ./  